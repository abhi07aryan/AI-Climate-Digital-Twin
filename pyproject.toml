[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "climate_twin"
version = "0.1.0"
description = "AI Powered Climate Digital Twin"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]